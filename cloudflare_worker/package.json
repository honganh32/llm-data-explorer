{
  "name": "llm-chart-mcp-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker port of the LLM Chart Plugin MCP server (stateless, Streamable HTTP).",
  "scripts": {
    "gen": "node scripts/build-plugins.mjs",
    "dev": "npm run gen && wrangler dev",
    "deploy": "npm run gen && wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "agents": "^0.17.4",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260708.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.110.0"
  }
}
