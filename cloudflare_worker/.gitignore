node_modules/
.wrangler/
.dev.vars
dist/
# src/plugins.json is generated by scripts/build-plugins.mjs (npm run gen).
# It is committed so the repo is deployable without a build step, but it is
# regenerated automatically before every `npm run dev` / `npm run deploy`.
